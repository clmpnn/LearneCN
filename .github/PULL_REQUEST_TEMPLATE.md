## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## How you tested it

<!-- Which page, which browser. "Served from the repo root and clicked through
     Practice on HSK 4" is enough. -->

## Checks

- [ ] `python3 tools/validate.py` passes.
- [ ] Served from the **repository root**, not from `html/`, and the pages load their data.
- [ ] Any new `.html`, `.css` or `.js` file carries the GPL notice
      (`python3 tools/apply-license-headers.py` adds it).
- [ ] Nothing new was added under `vendor/` without its upstream licence intact.
- [ ] If this adds or edits questions: four distinct choices, exactly one correct,
      and the question text is in Chinese.

## Licence

By opening this pull request I agree that my contribution is licensed
**GPL-3.0-or-later**, matching the rest of the project.
